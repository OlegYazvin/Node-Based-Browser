name: Build macOS Installers

on:
  push:
    branches:
      - main
    paths-ignore:
      - "Installer/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos-installers:
    name: Build macOS installer (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Intel
            os: macos-15-intel
            arch: x64
          - label: Apple Silicon
            os: macos-15
            arch: arm64

    defaults:
      run:
        shell: bash

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Resolve published Gecko ESR release
        run: node gecko/scripts/resolve-supported-esr-release.mjs >> "$GITHUB_ENV"

      - name: Install npm dependencies
        run: npm ci

      - name: Choose Gecko checkout directory
        run: echo "GECKO_CHECKOUT_DIR=$RUNNER_TEMP/firefox-esr" >> "$GITHUB_ENV"

      - name: Bootstrap Gecko ESR checkout
        run: |
          set -o pipefail
          if ! npm run gecko:bootstrap -- --checkout-dir "$GECKO_CHECKOUT_DIR" --ref "$FIREFOX_ESR_REF" 2>&1 | tee gecko-bootstrap.log; then
            summary="$(node scripts/summarize-ci-log.mjs --log gecko-bootstrap.log --mode bootstrap)"
            echo "::error title=Gecko bootstrap failed::$summary"
            exit 1
          fi

      - name: Write Gecko mozconfig
        run: npm run gecko:mozconfig -- --checkout-dir "$GECKO_CHECKOUT_DIR"

      - name: Build Gecko overlay target
        env:
          MOZCONFIG: mozconfig.nodely
          NODEJS: node
        run: |
          set -o pipefail
          if ! (
            cd "$GECKO_CHECKOUT_DIR"
            python ./mach configure
            python ./mach artifact install
            cd "$GITHUB_WORKSPACE"
            npm run gecko:sync -- --checkout-dir "$GECKO_CHECKOUT_DIR"
            npm run gecko:refresh-branding -- --checkout-dir "$GECKO_CHECKOUT_DIR" --mode compat
            cd "$GECKO_CHECKOUT_DIR"
            python ./mach build faster
          ) 2>&1 | tee gecko-build.log; then
            summary="$(node scripts/summarize-ci-log.mjs --log gecko-build.log --mode build)"
            echo "::error title=Gecko overlay build failed::$summary"
            exit 1
          fi

      - name: Refresh artifact-build Nodely branding
        run: npm run gecko:refresh-branding -- --checkout-dir "$GECKO_CHECKOUT_DIR"

      - name: Validate macOS app bundle before packaging
        run: |
          set -euo pipefail
          dist_dir="$GECKO_CHECKOUT_DIR/obj-nodely/dist"
          app_bundle_count="$(find "$dist_dir" -type d -name '*.app' | wc -l | tr -d ' ')"

          if [[ "$app_bundle_count" == "0" ]]; then
            echo "No macOS app bundles found under $dist_dir." >&2
            exit 1
          fi

          while IFS= read -r app_bundle; do
            info_plist="$app_bundle/Contents/Info.plist"

            if [[ ! -f "$info_plist" ]]; then
              echo "Missing Info.plist in $app_bundle." >&2
              exit 1
            fi

            plutil -lint "$info_plist" >/dev/null
            executable_name="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleExecutable' "$info_plist")"
            executable_path="$app_bundle/Contents/MacOS/$executable_name"

            if [[ ! -x "$executable_path" ]]; then
              echo "Missing macOS app executable $executable_path." >&2
              exit 1
            fi

            echo "Validated $app_bundle"
          done < <(find "$dist_dir" -type d -name '*.app' | sort)

      - name: Package Gecko build
        env:
          MOZCONFIG: mozconfig.nodely
          NODEJS: node
        run: |
          set -o pipefail
          if ! (
            cd "$GECKO_CHECKOUT_DIR"
            python ./mach package
          ) 2>&1 | tee gecko-package.log; then
            summary="$(node scripts/summarize-ci-log.mjs --log gecko-package.log --mode package)"
            echo "::error title=Gecko package failed::$summary"
            exit 1
          fi

      - name: Stage Gecko release artifact
        run: |
          set -o pipefail
          if ! npm run gecko:stage-release -- --checkout-dir "$GECKO_CHECKOUT_DIR" --platform darwin --arch ${{ matrix.arch }} --channel ci 2>&1 | tee gecko-stage-release.log; then
            summary="$(node scripts/summarize-ci-log.mjs --log gecko-stage-release.log --mode stage)"
            echo "::error title=Gecko release artifact staging failed::$summary"
            exit 1
          fi

      - name: Build macOS installer
        run: |
          set -o pipefail
          if ! npm run installers -- --platform darwin --arch ${{ matrix.arch }} --channel ci --no-sync --strict 2>&1 | tee installer-build.log; then
            summary="$(node scripts/summarize-ci-log.mjs --log installer-build.log --mode installer)"
            echo "::error title=macOS installer build failed::$summary"
            exit 1
          fi

      - name: Verify packaged macOS DMG payload
        run: |
          set -euo pipefail
          shopt -s nullglob
          dmgs=(out/make/darwin/${{ matrix.arch }}/*.dmg)

          if [[ ${#dmgs[@]} -eq 0 ]]; then
            echo "No macOS DMG outputs were found to verify." >&2
            exit 1
          fi

          for dmg in "${dmgs[@]}"; do
            echo "Verifying $dmg"
            mount_dir="$(mktemp -d "${RUNNER_TEMP:-/tmp}/nodely-dmg-XXXXXX")"
            hdiutil attach -nobrowse -readonly -mountpoint "$mount_dir" "$dmg" >/dev/null
            app_bundle="$(find "$mount_dir" -maxdepth 1 -type d -name '*.app' | sort | head -n 1)"

            if [[ -z "$app_bundle" ]]; then
              hdiutil detach "$mount_dir" -force >/dev/null || true
              rm -rf "$mount_dir"
              echo "No macOS app bundle was found inside $dmg." >&2
              exit 1
            fi

            codesign --verify --deep --strict --verbose=2 "$app_bundle"
            hdiutil detach "$mount_dir" -force >/dev/null
            rm -rf "$mount_dir"
          done

      - name: Derive macOS release metadata
        id: release_meta
        run: |
          node --input-type=module <<'NODE'
          import { appendFileSync, readdirSync, statSync } from "node:fs";
          import path from "node:path";

          import { extractInstallerVersion } from "./scripts/installers-lib.mjs";
          import { readNodelyVersionMetadata } from "./scripts/nodely-version.mjs";

          const outputDirectory = path.join("out", "make", "darwin", "${{ matrix.arch }}");
          const fileNames = readdirSync(outputDirectory)
            .filter((name) => name.startsWith("Nodely-Browser-"))
            .filter((name) => /\.(?:dmg|pkg)$/iu.test(name))
            .sort();

          if (!fileNames.length) {
            throw new Error(`No macOS installer assets were found in ${outputDirectory}.`);
          }

          const assets = fileNames
            .map((name) => path.join(outputDirectory, name))
            .filter((assetPath) => statSync(assetPath).size > 0)
            .map((assetPath) => assetPath.replaceAll(path.sep, path.posix.sep));

          if (!assets.length) {
            throw new Error(`macOS installer assets in ${outputDirectory} were empty.`);
          }

          const versions = [...new Set(fileNames.map((name) => extractInstallerVersion(name)).filter(Boolean))];

          if (versions.length !== 1) {
            throw new Error(`Expected exactly one macOS installer version, found: ${versions.join(", ")}`);
          }

          const version = versions[0];
          const { displayVersion } = readNodelyVersionMetadata();

          if (version !== displayVersion) {
            throw new Error(`Built macOS installer version ${version} does not match package.json Nodely version ${displayVersion}.`);
          }

          const outputPath = process.env.GITHUB_OUTPUT;

          appendFileSync(outputPath, `version=${version}\n`);
          appendFileSync(outputPath, `gecko_version=${process.env.FIREFOX_ESR_VERSION ?? ""}\n`);
          appendFileSync(outputPath, `tag=nodely-${version}\n`);
          appendFileSync(outputPath, `title=Nodely ${version}\n`);
          appendFileSync(outputPath, "assets<<EOF\n");
          appendFileSync(outputPath, `${assets.join("\n")}\n`);
          appendFileSync(outputPath, "EOF\n");
          NODE

      - name: Publish macOS release assets
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.release_meta.outputs.tag }}
          RELEASE_TITLE: ${{ steps.release_meta.outputs.title }}
          RELEASE_VERSION: ${{ steps.release_meta.outputs.version }}
          RELEASE_ASSETS: ${{ steps.release_meta.outputs.assets }}
        run: |
          if ! gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            cat > macos-release-notes.md <<EOF
          Refreshed macOS installer assets for Nodely ${RELEASE_VERSION}.
          Gecko base version: ${{ steps.release_meta.outputs.gecko_version }}.

          Generated by ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}.
          EOF
            gh release create "$RELEASE_TAG" \
              --title "$RELEASE_TITLE" \
              --notes-file macos-release-notes.md \
              --target "$GITHUB_SHA"
          fi

          while IFS= read -r asset; do
            if [[ -n "$asset" ]]; then
              gh release upload "$RELEASE_TAG" "$asset" --clobber
            fi
          done <<< "$RELEASE_ASSETS"

      - name: Report macOS installer outputs
        run: |
          node --input-type=module <<'NODE'
          import { readdirSync, statSync } from "node:fs";
          import path from "node:path";

          const outputDirectory = path.join("out", "make", "darwin", "${{ matrix.arch }}");

          for (const name of readdirSync(outputDirectory).sort()) {
            const assetPath = path.join(outputDirectory, name);

            if (statSync(assetPath).isFile()) {
              console.log(`Installer output: ${name}`);
            }
          }
          NODE
          echo "macOS installer verification uploads artifacts and refreshes the matching GitHub Release assets directly."

      - name: Upload failure logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: macos-installer-failure-logs-${{ matrix.arch }}
          path: |
            gecko-bootstrap.log
            gecko-build.log
            gecko-package.log
            gecko-stage-release.log
            installer-build.log
          if-no-files-found: ignore

      - name: Upload macOS installers
        uses: actions/upload-artifact@v4
        with:
          name: macos-installers-${{ matrix.arch }}
          path: out/make/darwin/${{ matrix.arch }}
          if-no-files-found: error
